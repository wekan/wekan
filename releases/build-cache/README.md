# Build Cache to Speed Up Local Builds

- npm package registry cache: verdaccion

- http downloads cache: Caddy (frankenphp, because it's easier to download one file. Not all of apt install required)
