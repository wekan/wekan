name: Release All Platforms

# Triggered manually via GitHub CLI or Actions UI.
# Usage:
#   gh workflow run release-all.yml -f old_version=8.42 -f new_version=8.43
#
# Or use the convenience script (commits pending changes, pushes, and triggers):
#   ./releases/release-all.sh 8.42 8.43
#
# Required GitHub Secrets:
#   GHCR_AUTH      - base64(username:token) for ghcr.io
#   DOCKERHUB_AUTH - base64(username:token) for Docker Hub
#   QUAY_AUTH      - base64(username:token) for Quay.io
#   SNAP_AUTH      - Snapcraft store credentials

on:
  workflow_dispatch:
    inputs:
      old_version:
        description: 'Previous version number (e.g. 8.42, without v prefix)'
        required: true
        type: string
      new_version:
        description: 'New version number (e.g. 8.43, without v prefix)'
        required: true
        type: string

permissions:
  contents: write
  packages: write
  pages: write
  id-token: write

jobs:
  # ─── 0. Version bump, docs rebuild, and tag creation ────────────────────────
  # Runs update-node-version.sh, rebuild-docs.sh, and sed-release-versions.sh,
  # then commits, pushes, and creates the version tag.
  # Skipped on re-run if the tag already exists.
  version-bump:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Check if tag already exists
        id: check
        run: |
          if git ls-remote --tags origin | grep -q "refs/tags/v${{ inputs.new_version }}$"; then
            echo "existed=true" >> $GITHUB_OUTPUT
            echo "Tag v${{ inputs.new_version }} already exists. Skipping version bump."
          else
            echo "existed=false" >> $GITHUB_OUTPUT
          fi

      - uses: actions/setup-node@v6
        if: steps.check.outputs.existed == 'false'
        with:
          node-version: '24.14.1'

      - name: Install Python dependencies for API doc generation
        if: steps.check.outputs.existed == 'false'
        run: python3 -m pip install --break-system-packages esprima

      - name: Delete GitHub Actions caches
        if: steps.check.outputs.existed == 'false'
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh cache delete --all || true

      - name: Update Node.js to latest 24.x
        if: steps.check.outputs.existed == 'false'
        run: ./releases/update-node-version.sh

      - name: Rebuild API docs
        if: steps.check.outputs.existed == 'false'
        run: ./releases/rebuild-docs.sh "${{ inputs.new_version }}"

      - name: Update version numbers in release files
        if: steps.check.outputs.existed == 'false'
        run: ./releases/sed-release-versions.sh "${{ inputs.old_version }}" "${{ inputs.new_version }}"

      - name: Commit and push version bump
        if: steps.check.outputs.existed == 'false'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add --all
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "v${{ inputs.new_version }}"
          fi
          git push

      - name: Create and push tag
        if: steps.check.outputs.existed == 'false'
        run: |
          git tag -a "v${{ inputs.new_version }}" -m "v${{ inputs.new_version }}"
          git push origin "v${{ inputs.new_version }}"

  # ─── 1. Extract version and release notes ───────────────────────────────────
  prepare:
    needs: version-bump
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      changelog: ${{ steps.changelog.outputs.content }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: refs/tags/v${{ inputs.new_version }}

      - name: Set version from input
        id: version
        run: |
          VERSION="${{ inputs.new_version }}"
          VERSION="${VERSION#v}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Extract release notes from CHANGELOG.md
        id: changelog
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          # Try to find the version-specific section first, then fall back to Upcoming
          NOTES=$(python3 - <<PYEOF
          import re, sys
          version = "$VERSION"
          with open("CHANGELOG.md", encoding="utf-8") as f:
              content = f.read()
          # Pattern: section heading through (but not including) the next heading
          patterns = [
              rf"(# v{re.escape(version)} .*?)(?=\n# v[0-9]|\Z)",
              r"(# Upcoming WeKan.*?)(?=\n# v[0-9]|\Z)",
          ]
          for pat in patterns:
              m = re.search(pat, content, re.DOTALL)
              if m:
                  print(m.group(1).strip())
                  sys.exit(0)
          print(f"WeKan v{version}")
          PYEOF
          )
          echo "content<<GHEOF" >> $GITHUB_OUTPUT
          echo "$NOTES" >> $GITHUB_OUTPUT
          echo "GHEOF" >> $GITHUB_OUTPUT

  # ─── 2. Build amd64 bundle (full Meteor 3 build) ────────────────────────────
  # Meteor 3.5-beta.7 installer supports only: Linux x86_64, Linux aarch64, macOS, Windows.
  # It explicitly rejects armhf, s390x, and ppc64le at install time.
  # Therefore the full Meteor build always runs here on x86_64.
  # All other Linux architectures then rebuild only the native npm modules
  # from this bundle (much faster than a full Meteor build).
  # Node.js version must match what Meteor 3.5-beta.7 bundles: 24.14.1 (ABI 143).
  build-amd64:
    needs: prepare
    runs-on: ubuntu-24.04
    timeout-minutes: 180
    env:
      VERSION: ${{ needs.prepare.outputs.version }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: refs/tags/v${{ inputs.new_version }}

      - name: Cache Meteor installation
        uses: actions/cache@v5
        with:
          path: ~/.meteor
          key: meteor-${{ hashFiles('.meteor/release') }}-${{ hashFiles('.meteor/packages') }}
          restore-keys: meteor-

      - name: Cache npm packages
        uses: actions/cache@v5
        with:
          path: ~/.npm
          key: npm-${{ hashFiles('package-lock.json') }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential g++ make python3 curl wget zip unzip

      # Pin to Node 24.14.1 — the exact version bundled by Meteor 3.5-beta.7.
      # Using the same version for build and npm rebuild ensures consistent native module ABI.
      - uses: actions/setup-node@v6
        with:
          node-version: '24.14.1'

      - name: Install Meteor
        run: |
          if [ ! -f ~/.meteor/meteor ]; then
            curl https://install.meteor.com/ | sh
          fi
          echo "$HOME/.meteor" >> $GITHUB_PATH

      - name: Build WeKan bundle
        run: |
          rm -rf .build node_modules .meteor/local
          (meteor update --npm 2>/dev/null || true) && meteor npm install
          meteor build .build --directory

      - name: Create amd64 zip
        run: |
          cd .build
          zip -r wekan-${VERSION}-amd64.zip bundle

      - uses: actions/upload-artifact@v7
        with:
          name: bundle-amd64
          path: .build/wekan-${{ needs.prepare.outputs.version }}-amd64.zip
          retention-days: 1

  # ─── 3. Build arm64 bundle (rebuild native modules natively) ─────────────────
  # Meteor 3.5-beta.7 does support Linux aarch64, but running a full Meteor build a
  # second time would double the build time for the same result. npm rebuild on
  # the native arm64 GitHub Actions runner is much faster and produces the same
  # platform-specific native modules.
  build-arm64:
    needs: [prepare, build-amd64]
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 60
    env:
      VERSION: ${{ needs.prepare.outputs.version }}
    steps:
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential g++ make python3 curl unzip zip

      - uses: actions/setup-node@v6
        with:
          node-version: '24.14.1'

      - name: Download amd64 bundle
        uses: actions/download-artifact@v8
        with:
          name: bundle-amd64

      - name: Rebuild native modules for arm64
        run: |
          unzip wekan-${VERSION}-amd64.zip
          # Fix: bcrypt 5.0.1 (Meteor accounts-password) ships node-addon-api@3
          # which fails to compile on Node.js 24. Replace with bcrypt 6.0.0.
          BCRYPT_DIR="$(pwd)/bundle/programs/server/npm/node_modules/meteor/accounts-password/node_modules/bcrypt"
          if [ -d "$BCRYPT_DIR" ]; then
            rm -rf "$BCRYPT_DIR"
            TMP=$(mktemp -d)
            pushd "$TMP" > /dev/null
            npm pack bcrypt@6.0.0
            mkdir bcrypt && tar xzf bcrypt-*.tgz --strip-components=1 -C bcrypt
            cd bcrypt && npm install --ignore-scripts && cd ..
            cp -a bcrypt "$BCRYPT_DIR"
            popd > /dev/null
            rm -rf "$TMP"
          fi
          cd bundle/programs/server
          rm -rf node_modules
          npm install
          cd ../..

      - name: Clean up and create arm64 zip
        run: |
          zip -r wekan-${VERSION}-arm64.zip bundle

      - uses: actions/upload-artifact@v7
        with:
          name: bundle-arm64
          path: wekan-${{ needs.prepare.outputs.version }}-arm64.zip
          retention-days: 1

  # ─── 4. Build s390x bundle (rebuild native modules via QEMU container) ───────
  # Meteor 3.5-beta.7 does NOT support s390x (installer exits with error on that arch).
  # GitHub Actions has no native s390x runners.
  # Solution: QEMU emulation + node:24.14.1-slim (s390x variant) for npm rebuild.
  # node:24.14.1-slim is pinned to match Meteor 3.5-beta.7's bundled Node.js version exactly.
  build-s390x:
    needs: [prepare, build-amd64]
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      VERSION: ${{ needs.prepare.outputs.version }}
    steps:
      - name: Set up QEMU for s390x
        uses: docker/setup-qemu-action@v4
        with:
          platforms: s390x

      - name: Install unzip/zip
        run: sudo apt-get update && sudo apt-get install -y unzip zip

      - name: Download amd64 bundle
        uses: actions/download-artifact@v8
        with:
          name: bundle-amd64

      - name: Rebuild native modules for s390x in container
        run: |
          unzip wekan-${VERSION}-amd64.zip
          rm -rf bundle/programs/server/node_modules
          # Fix: bcrypt 5.0.1 (Meteor accounts-password) ships node-addon-api@3
          # which fails to compile on Node.js 24. Replace with bcrypt 6.0.0.
          BCRYPT_DIR="$(pwd)/bundle/programs/server/npm/node_modules/meteor/accounts-password/node_modules/bcrypt"
          if [ -d "$BCRYPT_DIR" ]; then
            rm -rf "$BCRYPT_DIR"
            TMP=$(mktemp -d)
            pushd "$TMP" > /dev/null
            npm pack bcrypt@6.0.0
            mkdir bcrypt && tar xzf bcrypt-*.tgz --strip-components=1 -C bcrypt
            cd bcrypt && npm install --ignore-scripts && cd ..
            cp -a bcrypt "$BCRYPT_DIR"
            popd > /dev/null
            rm -rf "$TMP"
          fi
          # node:24.14.1-slim s390x — same Node.js version as Meteor 3.5-beta.7 bundled
          docker run --rm --platform linux/s390x \
            -v "$(pwd)/bundle:/bundle" \
            -w /bundle/programs/server \
            node:24.14.1-slim \
            sh -c "apt-get update -qq && apt-get install -y -q build-essential python3 && npm install"

      - name: Clean up and create s390x zip
        run: |
          zip -r wekan-${VERSION}-s390x.zip bundle

      - uses: actions/upload-artifact@v7
        with:
          name: bundle-s390x
          path: wekan-${{ needs.prepare.outputs.version }}-s390x.zip
          retention-days: 1

  # ─── 4b. Build ppc64le bundle (rebuild native modules via QEMU container) ────
  # Meteor 3.5-beta.7 does NOT support ppc64le (installer exits with error on that arch).
  # GitHub Actions has no native ppc64le runners.
  # Solution: QEMU emulation + node:24.14.1-slim (ppc64le variant) for npm rebuild.
  build-ppc64le:
    needs: [prepare, build-amd64]
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      VERSION: ${{ needs.prepare.outputs.version }}
    steps:
      - name: Set up QEMU for ppc64le
        uses: docker/setup-qemu-action@v4
        with:
          platforms: ppc64le

      - name: Install unzip/zip
        run: sudo apt-get update && sudo apt-get install -y unzip zip

      - name: Download amd64 bundle
        uses: actions/download-artifact@v8
        with:
          name: bundle-amd64

      - name: Rebuild native modules for ppc64le in container
        run: |
          unzip wekan-${VERSION}-amd64.zip
          rm -rf bundle/programs/server/node_modules
          # Fix: bcrypt 5.0.1 (Meteor accounts-password) ships node-addon-api@3
          # which fails to compile on Node.js 24. Replace with bcrypt 6.0.0.
          BCRYPT_DIR="$(pwd)/bundle/programs/server/npm/node_modules/meteor/accounts-password/node_modules/bcrypt"
          if [ -d "$BCRYPT_DIR" ]; then
            rm -rf "$BCRYPT_DIR"
            TMP=$(mktemp -d)
            pushd "$TMP" > /dev/null
            npm pack bcrypt@6.0.0
            mkdir bcrypt && tar xzf bcrypt-*.tgz --strip-components=1 -C bcrypt
            cd bcrypt && npm install --ignore-scripts && cd ..
            cp -a bcrypt "$BCRYPT_DIR"
            popd > /dev/null
            rm -rf "$TMP"
          fi
          # node:24.14.1-slim ppc64le — same Node.js version as Meteor 3.5-beta.7 bundled
          docker run --rm --platform linux/ppc64le \
            -v "$(pwd)/bundle:/bundle" \
            -w /bundle/programs/server \
            node:24.14.1-slim \
            sh -c "apt-get update -qq && apt-get install -y -q build-essential python3 && npm install"

      - name: Clean up and create ppc64le zip
        run: |
          zip -r wekan-${VERSION}-ppc64le.zip bundle

      - uses: actions/upload-artifact@v7
        with:
          name: bundle-ppc64le
          path: wekan-${{ needs.prepare.outputs.version }}-ppc64le.zip
          retention-days: 1

  # ─── 4c. Build armhf bundle — COMMENTED OUT ─────────────────────────────────
  # No official Node.js binaries exist for armhf (arm/v7).
  #build-armhf:
  #  needs: [prepare, build-amd64]
  #  runs-on: ubuntu-latest
  #  timeout-minutes: 120
  #  env:
  #    VERSION: ${{ needs.prepare.outputs.version }}
  #  steps:
  #    - name: Set up QEMU for arm/v7
  #      uses: docker/setup-qemu-action@v4
  #      with:
  #        platforms: arm
  #
  #    - name: Install unzip/zip
  #      run: sudo apt-get update && sudo apt-get install -y unzip zip
  #
  #    - name: Download amd64 bundle
  #      uses: actions/download-artifact@v8
  #      with:
  #        name: bundle-amd64
  #
  #    - name: Rebuild native modules for armhf in container
  #      run: |
  #        unzip wekan-${VERSION}-amd64.zip
  #        rm -rf bundle/programs/server/node_modules
  #        # Fix: bcrypt 5.0.1 (Meteor accounts-password) ships node-addon-api@3
  #        # which fails to compile on Node.js 24. Replace with bcrypt 6.0.0.
  #        BCRYPT_DIR="$(pwd)/bundle/programs/server/npm/node_modules/meteor/accounts-password/node_modules/bcrypt"
  #        if [ -d "$BCRYPT_DIR" ]; then
  #          rm -rf "$BCRYPT_DIR"
  #          TMP=$(mktemp -d)
  #          pushd "$TMP" > /dev/null
  #          npm pack bcrypt@6.0.0
  #          mkdir bcrypt && tar xzf bcrypt-*.tgz --strip-components=1 -C bcrypt
  #          cd bcrypt && npm install --ignore-scripts && cd ..
  #          cp -a bcrypt "$BCRYPT_DIR"
  #          popd > /dev/null
  #          rm -rf "$TMP"
  #        fi
  #        # node:24.14.1-slim arm/v7 — same Node.js version as Meteor 3.5-beta.7 bundled
  #        docker run --rm --platform linux/arm/v7 \
  #          -v "$(pwd)/bundle:/bundle" \
  #          -w /bundle/programs/server \
  #          node:24.14.1-slim \
  #          sh -c "apt-get update -qq && apt-get install -y -q build-essential python3 && npm install"
  #
  #    - name: Clean up and create armhf zip
  #      run: |
  #        zip -r wekan-${VERSION}-armhf.zip bundle
  #
  #    - uses: actions/upload-artifact@v7
  #      with:
  #        name: bundle-armhf
  #        path: wekan-${{ needs.prepare.outputs.version }}-armhf.zip
  #        retention-days: 1

  # ─── 5. Build win64 bundle (rebuild native modules on Windows) ───────────────
  build-win64:
    needs: [prepare, build-amd64]
    runs-on: windows-latest
    timeout-minutes: 60
    env:
      VERSION: ${{ needs.prepare.outputs.version }}
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: '24.14.1'

      ## With Node 24 these are already preinstalled at GitHub Windows runners
      #- name: Install node-gyp and build tools
      #  run: |
      #    npm install -g windows-build-tools --vs2019 2>$null; exit 0

      - name: Install 7-zip
        run: choco install -y 7zip

      - name: Download amd64 bundle
        uses: actions/download-artifact@v8
        with:
          name: bundle-amd64

      - name: Fix bcrypt for Node.js 24
        shell: bash
        run: |
          7z x wekan-${VERSION}-amd64.zip
          # Fix: bcrypt 5.0.1 (Meteor accounts-password) ships node-addon-api@3
          # which fails to compile on Node.js 24. Replace with bcrypt 6.0.0.
          BCRYPT_DIR="$(pwd)/bundle/programs/server/npm/node_modules/meteor/accounts-password/node_modules/bcrypt"
          if [ -d "$BCRYPT_DIR" ]; then
            rm -rf "$BCRYPT_DIR"
            TMP=$(mktemp -d)
            pushd "$TMP" > /dev/null
            npm pack bcrypt@6.0.0
            mkdir bcrypt && tar xzf bcrypt-*.tgz --strip-components=1 -C bcrypt
            cd bcrypt && npm install --ignore-scripts && cd ..
            cp -r bcrypt "$BCRYPT_DIR"
            popd > /dev/null
            rm -rf "$TMP"
          fi

      - name: Rebuild native modules for Windows
        shell: cmd
        run: |
          cd bundle\programs\server
          rmdir /s /q node_modules
          npm install
          cd ..\..\..

      - name: Create win64 zip
        shell: cmd
        run: 7z a wekan-%VERSION%-win64.zip bundle

      - uses: actions/upload-artifact@v7
        with:
          name: bundle-win64
          path: wekan-${{ needs.prepare.outputs.version }}-win64.zip
          retention-days: 1

  # ─── 6. Create GitHub Release and upload all bundles ────────────────────────
  release:
    needs: [prepare, build-amd64, build-arm64, build-s390x, build-ppc64le, build-win64]
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.prepare.outputs.version }}
    steps:
      - name: Download all bundles
        uses: actions/download-artifact@v8
        with:
          pattern: bundle-*
          merge-multiple: true

      - name: Create GitHub Release with all bundles
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ needs.prepare.outputs.version }}
          name: v${{ needs.prepare.outputs.version }}
          body: ${{ needs.prepare.outputs.changelog }}
          files: |
            wekan-${{ needs.prepare.outputs.version }}-amd64.zip
            wekan-${{ needs.prepare.outputs.version }}-arm64.zip
            wekan-${{ needs.prepare.outputs.version }}-s390x.zip
            wekan-${{ needs.prepare.outputs.version }}-ppc64le.zip
            wekan-${{ needs.prepare.outputs.version }}-win64.zip

  # ─── 7. Build and push multi-platform Docker image ──────────────────────────
  docker:
    needs: [prepare, release]
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.prepare.outputs.version }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: refs/tags/v${{ inputs.new_version }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Create Docker config with registry credentials
        run: |
          mkdir -p ~/.docker
          cat > ~/.docker/config.json <<EOF
          {
            "auths": {
              "ghcr.io": {
                "auth": "${{ secrets.GHCR_AUTH }}"
              },
              "https://index.docker.io/v1/": {
                "auth": "${{ secrets.DOCKERHUB_AUTH }}"
              },
              "quay.io": {
                "auth": "${{ secrets.QUAY_AUTH }}"
              }
            }
          }
          EOF

      - name: Create and use buildx builder
        run: |
          docker buildx create --name mybuilder --driver docker-container --use
          docker buildx inspect --bootstrap

      - name: Build and push Docker image to all registries
        run: |
          docker buildx build \
            --platform linux/amd64,linux/arm64,linux/s390x,linux/ppc64le \
            -t wekanteam/wekan:v${VERSION} \
            -t wekanteam/wekan:latest \
            -t quay.io/wekan/wekan:v${VERSION} \
            -t quay.io/wekan/wekan:latest \
            -t ghcr.io/wekan/wekan:v${VERSION} \
            -t ghcr.io/wekan/wekan:latest \
            --push .

  # ─── 8. Build and publish snap packages ──────────────────────────────────────
  # Build snap with snapcraft pack (like releases/snap-build.sh) using LXD,
  # then push to Snap Store edge channel (like releases/snap-push-to-store.sh).
  # Promote to stable manually via the Snap Store dashboard when ready.
  snap:
    needs: [prepare, release]
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.prepare.outputs.version }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: refs/tags/v${{ inputs.new_version }}

      - name: Install snapcraft
        run: sudo snap install snapcraft --classic

      - name: Build snap
        run: sudo snapcraft pack --destructive-mode

      - name: Upload snap files as artifacts
        uses: actions/upload-artifact@v7
        with:
          name: snaps
          path: "*.snap"
          retention-days: 30

      - name: Push all snaps to Snap Store edge channel
        env:
          SNAPCRAFT_STORE_CREDENTIALS: ${{ secrets.SNAP_AUTH }}
        run: |
          for snap_file in *.snap; do
            echo "Publishing $snap_file ..."
            snapcraft upload --release=edge "$snap_file"
          done

      - name: Upload all snaps to GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ needs.prepare.outputs.version }}
          files: "*.snap"

  # ─── 9. Update Helm charts and deploy to GitHub Pages ─────────────────────────
  # Chart source is in helm/wekan/, published packages + index.yaml in helm/charts/.
  # GitHub Pages deploys helm/charts/ via Actions, serving at https://wekan.github.io/wekan/
  helm:
    needs: [prepare, docker]
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.prepare.outputs.version }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: refs/tags/v${{ inputs.new_version }}

      - name: Install Helm
        run: |
          curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

      - name: Update Chart.yaml and values.yaml with new version
        run: |
          cd helm/wekan
          # Chart.yaml: update appVersion and version
          sed -i "s/^appVersion: .*/appVersion: \"${VERSION}\"/" Chart.yaml
          sed -i "s/^version: .*/version: ${VERSION}.0/" Chart.yaml
          # values.yaml: update image tag
          sed -i "s/^  tag: v.*/  tag: v${VERSION}/" values.yaml

      - name: Build Helm dependencies
        run: |
          cd helm/wekan
          helm repo add mongo https://groundhog2k.github.io/helm-charts/
          helm dependency update
          helm dependency build

      - name: Package chart and update index
        run: |
          helm package helm/wekan -d helm/charts/
          helm repo index helm/charts/ --url https://wekan.github.io/wekan/

      - name: Commit updated chart
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add helm/
          git commit -m "Helm chart ${VERSION}.0"
          git push origin HEAD:refs/heads/main

      - name: Upload helm/charts/ as Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: helm/charts/

  # ─── 10. Deploy Helm chart to GitHub Pages ───────────────────────────────────
  deploy-pages:
    needs: helm
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
