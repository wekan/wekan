'use strict';
const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..');const read=c=>JSON.parse(fs.readFileSync(path.join(root,`imports/i18n/data/${c}.i18n.json`),'utf8'));const tig=read('tig'),ti=read('ti');const terms={'changeFontPopup-title':'ሸክል ፊደል','change-font':'ሸክል ፊደል',font:'ሸክል ፊደል','invitePeoplePopup-title':'አዳም ዐዝም','invite-people':'አዳም ዐዝም','change-permissions':'እጃዝት ቀይር','changePermissionsPopup-title':'እጃዝት ቀይር'};for(const[k,v]of Object.entries(terms)){assert.equal(tig[k],v);assert.notEqual(tig[k],ti[k]);}assert.match(tig['invite-people'],/ ዐዝም$/);assert.match(tig['change-permissions'],/^እጃዝት /);console.log('Checked seven Tigre Font, Invite and permissions controls.');
