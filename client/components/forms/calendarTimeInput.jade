template(name="calendarTimeInput")
  .calendar-time-input
    input.js-calendar-native-time(type="hidden" id=inputId name=inputName class=fieldClass value=nativeTime)
    select.js-calendar-hour(aria-label="{{_ 'hours'}}")
      each hours
        option(value=value selected=selected) {{value}}
    span(aria-hidden="true") :
    select.js-calendar-minute(aria-label="{{_ 'minutes'}}")
      each minutes
        option(value=value selected=selected) {{value}}
