//- The pencil that renames the board, beside the board's name in the FIRST
//- header bar. The name is there, so the thing that edits it belongs next to it
//- rather than in a bar of its own. Board admins only; everybody else sees the
//- name without a pencil. docs/Design/Page/Header.md
template(name="boardEditTitleButton")
  if currentUser.isBoardAdmin
    with currentBoard
      a.board-header-btn.js-edit-board-title(title="{{_ 'edit'}}" value=title)
        i.fa.fa-pencil-square-o

//- Everything else the board's second header bar held, as icons in the FIRST
//- bar, to the left of the notification bell.
//-
//- ICON ONLY, each named by a tooltip. The bar is a single row shared with the
//- logo, the starred boards, the view menu, the bell and the user menu, and
//- these are ten more controls - labelled, they would not fit at any width.
//-
//- There is no desktop/mobile split any more either. The second bar wrote the
//- whole set out TWICE, once for each, because a labelled row does not fit on a
//- phone; icons do, so one set serves both. docs/Design/Page/Header.md
template(name="boardHeaderButtons")
  if currentBoard
    if currentUser
      .board-header-btns.board-header-controls
        with currentBoard
          a.board-header-btn(
            class="{{#if currentUser.isBoardAdmin}}js-change-visibility{{else}}is-disabled{{/if}}"
            title="{{_ currentBoard.permission}}")
            i.fa(class="{{#if currentBoard.isPublic}}fa-globe{{else}}fa-lock{{/if}}")
            //- The SAME expression the tooltip uses, so the two cannot say
              different things - and both follow the board from Private to
              Public without a second list of words to keep in step.
            span.board-header-btn-label {{_ currentBoard.permission}}

          unless watchDisabled
            a.board-header-btn.js-watch-board(title="{{_ watchLevel }}")
              if $eq watchLevel "watching"
                i.fa.fa-eye
              if $eq watchLevel "tracking"
                i.fa.fa-bell
              if $eq watchLevel "muted"
                i.fa.fa-bell-slash
              span.board-header-btn-label {{_ watchLevel }}

          //- The name beside the icon where the bar has room for it, dropped
            when it does not - one rule in header.css hides every label of this
            bar below the width they stop fitting at.
            docs/Design/Page/Header.md
          a.board-header-btn(title="{{#if isSortActive}}{{_ 'sort-is-on'}}{{else}}{{_ 'sort-cards'}}{{/if}}" class="js-sort-cards {{#if isSortActive }}emphasis{{/if}}")
            i.fa.fa-sort
            span.board-header-btn-label {{#if isSortActive}}{{_ 'sort-is-on'}}{{else}}{{_ 'sort-cards'}}{{/if}}
          if isSortActive
            a.board-header-btn-close.js-sort-reset(title="{{_ 'remove-sort'}}")
              i.fa.fa-times-thin

        +boardButtons

        a.board-header-btn.js-open-filter-view(
            title="{{#if Filter.isActive}}{{_ 'filter-on-desc'}}{{else}}{{_ 'filter'}}{{/if}}"
            class="{{#if Filter.isActive}}js-filter-active{{/if}}")
          i.fa.fa-filter
          span.board-header-btn-label {{#if Filter.isActive}}{{_ 'filter-on-desc'}}{{else}}{{_ 'filter'}}{{/if}}
        if Filter.isActive
          a.board-header-btn-close.js-filter-reset(title="{{_ 'filter-clear'}}")
            i.fa.fa-times-thin

        a.board-header-btn.js-open-search-view(title="{{_ 'search'}}")
          i.fa.fa-search
          span.board-header-btn-label {{_ 'search'}}

        a.board-header-btn.js-toggle-dependencies(
            title="{{#if currentBoard.showDependencies}}{{_ 'hide-dependencies'}}{{else}}{{_ 'show-dependencies'}}{{/if}}"
            class="{{#if currentBoard.showDependencies}}emphasis{{/if}}")
          i.fa.fa-link
          span.board-header-btn-label {{#if currentBoard.showDependencies}}{{_ 'hide-dependencies'}}{{else}}{{_ 'show-dependencies'}}{{/if}}

        if canModifyBoard
          a.board-header-btn.js-multiselection-activate(
              title="{{#if MultiSelection.isActive}}{{_ 'multi-selection-on'}}{{else}}{{_ 'multi-selection'}}{{/if}}"
              class="{{#if MultiSelection.isActive}}emphasis{{/if}}")
            i.fa.fa-check-square-o
            //- Named, like the board's Multi-Selection on All Boards: a
              check-box outline says nothing about what the button does.
            span.board-header-btn-label {{_ 'multi-selection'}}
          if MultiSelection.isActive
            a.board-header-btn-close.js-multiselection-reset(title="{{_ 'multi-selection-off'}}")
              i.fa.fa-times-thin
    else
      a.board-header-btn.js-log-in(title="{{_ 'log-in'}}")
        i.fa.fa-sign-in

//- `described-list`: every row is icon + name + description, and the three are laid
//- out as three left-aligned columns (popup.css), so the descriptions of all the
//- rows start at the same x whether or not a row carries the check mark.
template(name="boardVisibilityList")
  ul.pop-over-list.described-list
    li
      with "private"
        a.js-select-visibility
          i.fa.fa-lock
          | {{_ 'private'}}
          if visibilityCheck
            i.fa.fa-check
          span.sub-name {{_ 'private-desc'}}
    if notAllowPrivateVisibilityOnly
      li
        with "public"
          a.js-select-visibility
            i.fa.fa-globe
            | {{_ 'public'}}
            if visibilityCheck
              i.fa.fa-check
            span.sub-name {{_ 'public-desc'}}

template(name="boardChangeVisibilityPopup")
  +boardVisibilityList

template(name="boardChangeWatchPopup")
  ul.pop-over-list.described-list
    li
      with "watching"
        a.js-select-watch
          i.fa.fa-eye
          | {{_ 'watching'}}
          if watchCheck
            i.fa.fa-check
          span.sub-name {{_ 'watching-info'}}
    li
      with "tracking"
        a.js-select-watch
          i.fa.fa-bell
          | {{_ 'tracking'}}
          if watchCheck
            i.fa.fa-check
          span.sub-name {{_ 'tracking-info'}}
    li
      with "muted"
        a.js-select-watch
          i.fa.fa-bell-slash
          | {{_ 'muted'}}
          if watchCheck
            i.fa.fa-check
          span.sub-name {{_ 'muted-info'}}

template(name="boardChangeViewPopup")
  ul.pop-over-list
    li
      with "board-view-swimlanes"
        a.js-open-swimlanes-view
          i.fa.fa-th-large
          | {{_ 'swimlanes'}}
          if $eq Utils.boardView "board-view-swimlanes"
            i.fa.fa-check
    li
      with "board-view-lists"
        a.js-open-lists-view
          i.fa.fa-trello
          | {{_ 'board-view-lists'}}
          if $eq Utils.boardView "board-view-lists"
            i.fa.fa-check
    li
      with "board-view-cal"
        a.js-open-cal-view
          i.fa.fa-calendar
          | {{_ 'board-view-cal'}}
          if $eq Utils.boardView "board-view-cal"
            i.fa.fa-check
    li
      with "board-view-gantt"
        a.js-open-gantt-view
          i.fa.fa-bar-chart
          | {{_ 'board-view-gantt'}}
          if $eq Utils.boardView "board-view-gantt"
            i.fa.fa-check
    li
      with "board-view-table"
        a.js-open-table-view
          i.fa.fa-table
          | {{_ 'board-view-table'}}
          if $eq Utils.boardView "board-view-table"
            i.fa.fa-check
    li
      with "board-view-stats"
        a.js-open-stats-view
          i.fa.fa-pie-chart
          | {{_ 'board-view-stats'}}
          if $eq Utils.boardView "board-view-stats"
            i.fa.fa-check
    li
      with "board-view-map"
        a.js-open-map-view
          i.fa.fa-map-o
          | {{_ 'board-view-map'}}
          if $eq Utils.boardView "board-view-map"
            i.fa.fa-check

template(name="createBoard")
  form
    label
      | {{_ 'title'}}
      input.js-new-board-title(type="text" placeholder="{{_ 'bucket-example'}}" autofocus required)
    if visibilityMenuIsOpen
      +boardVisibilityList
    else
      p.quiet
        if $eq visibility 'public'
          span.fa.fa-globe.colorful
          = " "
          | {{{_ 'board-public-info'}}}
        else
          span.fa.fa-lock.colorful
          = " "
          | {{{_ 'board-private-info'}}}
        a.js-change-visibility {{_ 'change'}}.
    input.primary.wide(type="submit" value="{{_ 'create'}}")
    unless importDisabled
      span.quiet
        | {{_ 'or'}}
        a.js-import-board {{_ 'import'}}
    span.quiet
      | /
      a.js-board-template {{_ 'template'}}

template(name="createBoardPopup")
  form
    label
      | {{_ 'title'}}
      input.js-new-board-title(type="text" placeholder="{{_ 'bucket-example'}}" autofocus required)
    if visibilityMenuIsOpen
      +boardVisibilityList
    else
      p.quiet
        if $eq visibility 'public'
          span.fa.fa-globe.colorful
          = " "
          | {{{_ 'board-public-info'}}}
        else
          span.fa.fa-lock.colorful
          = " "
          | {{{_ 'board-private-info'}}}
        a.js-change-visibility {{_ 'change'}}.
    input.primary.wide(type="submit" value="{{_ 'create'}}")
    unless importDisabled
      span.quiet
        | {{_ 'or'}}
        a.js-import-board {{_ 'import'}}
    span.quiet
      | /
      a.js-board-template {{_ 'template'}}

template(name="headerBarCreateBoardPopup")
  form
    label
      | {{_ 'title'}}
      input.js-new-board-title(type="text" placeholder="{{_ 'bucket-example'}}" autofocus required)
    if visibilityMenuIsOpen
      +boardVisibilityList
    else
      p.quiet
        if $eq visibility 'public'
          span.fa.fa-globe.colorful
          = " "
          | {{{_ 'board-public-info'}}}
        else
          span.fa.fa-lock.colorful
          = " "
          | {{{_ 'board-private-info'}}}
        a.js-change-visibility {{_ 'change'}}.
    input.primary.wide(type="submit" value="{{_ 'create'}}")
    unless importDisabled
      span.quiet
        | {{_ 'or'}}
        a.js-import-board {{_ 'import'}}
    span.quiet
      | /
      a.js-board-template {{_ 'template'}}

// New popup for Template Container creation; shares the same form content
template(name="createTemplateContainerPopup")
  form
    label
      | {{_ 'title'}}
      input.js-new-board-title(type="text" placeholder="{{_ 'bucket-example'}}" autofocus required)
    if visibilityMenuIsOpen
      +boardVisibilityList
    else
      p.quiet
        if $eq visibility 'public'
          span.fa.fa-globe.colorful
          = " "
          | {{{_ 'board-public-info'}}}
        else
          span.fa.fa-lock.colorful
          = " "
          | {{{_ 'board-private-info'}}}
        a.js-change-visibility {{_ 'change'}}.
    input.primary.wide(type="submit" value="{{_ 'create'}}")
    unless importDisabled
      span.quiet
        | {{_ 'or'}}
        a.js-import-board {{_ 'import'}}
    span.quiet
      | /
      a.js-board-template {{_ 'template'}}

//template(name="listsortPopup")
//
  h2
//
   | {{_ 'list-sort-by'}}
//
  hr
//
  ul.pop-over-list
//
    each value in allowedSortValues
//
     li
//
      a.js-sort-by(name="{{value.name}}")
//
        if $eq sortby value.name
//
           i.fa(class="{{#if $eq Direction 'fa-arrow-up'}}fa-arrow-up{{else}}fa-arrow-down{{/if}}")
//
        | {{_ value.label }}{{_ value.shortLabel}}
//
        if $eq sortby value.name
//
           i.fa.fa-check
template(name="boardChangeTitlePopup")
  form
    label
      | {{_ 'title'}}
      input.js-board-name(type="text" value=title autofocus dir="auto")
    label
      | {{_ 'description'}}
      textarea.js-board-desc(dir="auto")= description
    input.primary.wide(type="submit" value="{{_ 'rename'}}")

template(name="boardCreateRulePopup")
  p {{_ 'close-board-pop'}}
  button.js-confirm.negate.full(type="submit") {{_ 'archive'}}


template(name="cardsSortPopup")
  ul.pop-over-list
    li
      a.js-sort-due
        i.fa.fa-calendar
        | {{_ 'due-date'}}
      hr
    li
      a.js-sort-title
        i.fa.fa-sort-alpha-asc
        | {{_ 'title-alphabetically'}}
      hr
    li
      a.js-sort-created-desc
        i.fa.fa-arrow-down
        | {{_ 'created-at-newest-first'}}
      hr
    li
      a.js-sort-created-asc
        i.fa.fa-arrow-up
        | {{_ 'created-at-oldest-first'}}


//- The board's view menu, in the FIRST top header bar.
//-
//- It was a button in the board's second header bar. A view menu says what you
//- are looking AT, so it belongs beside the page's name in the one bar that is
//- always on screen - the same place the All Boards Lists/Table menu is.
//-
//- `unless currentBoard.isTemplatesBoard`: there is nothing to look at six
//- ways on a templates board. docs/Design/Page/Header.md
//- Whether THIS board is starred, and the button that changes it. Its own
//- template because the first header bar places it beside the starred-boards
//- dropdown rather than among the board's other controls: the two are one pair -
//- how many boards you have starred, and whether this is one of them.
//-
//- The outline star means not starred and the solid one means starred, so the
//- button shows its state rather than the action - the tooltip says the action.
//- docs/Design/Page/Header.md
template(name="boardStarButton")
  if currentBoard
    a.board-header-btn.js-star-board(class="{{#if isStarred}}is-active{{/if}}"
      title="{{#if isStarred}}{{_ 'click-to-unstar'}}{{else}}{{_ 'click-to-star'}}{{/if}}")
      i.fa(class="fa-star{{#unless isStarred}}-o{{/unless}}")
      if showStarCounter
        span.board-star-counter {{currentBoard.stars}}

template(name="boardViewMenu")
  unless currentBoard.isTemplatesBoard
    //- The name is VISIBLE beside the icon, not only in the tooltip. Six view
      icons are six glyphs to learn, and a tooltip is the one place a name
      cannot be read without hovering - so the button says which view is on.
      The bar wraps to a second row when it runs out of width, which is what
      makes a word beside the icon affordable. docs/Design/Page/Header.md
    a.board-header-btn.js-toggle-board-view(title="{{boardViewName}}")
      i.fa.fa-caret-down
      if $eq boardView 'board-view-swimlanes'
        i.fa.fa-th-large
      if $eq boardView 'board-view-lists'
        i.fa.fa-trello
      if $eq boardView 'board-view-cal'
        i.fa.fa-calendar
      if $eq boardView 'board-view-gantt'
        i.fa.fa-bar-chart
      if $eq boardView 'board-view-table'
        i.fa.fa-table
      if $eq boardView 'board-view-stats'
        i.fa.fa-pie-chart
      if $eq boardView 'board-view-map'
        i.fa.fa-map-o
      span.board-header-btn-label= boardViewName
