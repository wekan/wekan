template(name="miniboard")
  .minicard(
    class="minicard-{{colorClass}}")
    .minicard-title
      .handle
        span.drag-handle(title="{{_ 'dragBoard'}}")
          i.fa.fa-arrows
      +viewer
        = title
