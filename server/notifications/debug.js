Meteor.startup(() => {
  // XXX: print to console, for debug purpose only
  // Notifications.subscribe('debug', (user, title, description, params) => {
  //  console.log(`${params.user}\n${TAPi18n.__(title, params)}\n${TAPi18n.__(description, params)}`);
  // });

  // XXX: more notification services can be added here

});
