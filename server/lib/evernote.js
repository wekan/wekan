//var Evernote = require('evernote').Evernote;
//var client = new Evernote.Client({
//  consumerKey: 'levensailor',
//  consumerSecret: '133ee554b0ebbd49',
//  sandbox: true// Optional (default: true)
//});
//client.getRequestToken('http://172.125.171.159/oath', function(error, oauthToken, oauthTokenSecret, results) {
  // store tokens in the session
  // and then redirect to client.getAuthorizeUrl(oauthToken)
//});
