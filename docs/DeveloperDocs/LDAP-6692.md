# LDAP bind investigation — issue 6692

Reviewed 2026-09-14. The issue and its empty comments API were read.
Snap 11.76 on Ubuntu reports a service-account LDAPS bind failure;
ldapsearch succeeds and reverting to 11.72 did not help.

Local commit `5f5822f7d` fixes the confirmed empty diagnostic: Error name
and message were lost by Object.keys-based sanitization. Diagnostics now
retain name, message, code and reason through existing secret redaction.
Certificate and invalid-credential tests pass; encryption, connection-release
and REST login suites also pass. TLS verification remains enabled.

The actual bind cause is unresolved. Current code accepts the deprecated
ssl setting as LDAPS and disables strict DN parsing for UPN binds. The
supplied error contains neither certificate nor LDAP result details, so no
TLS downgrade, credential rewrite or speculative dependency change was made.
No external directory or live browser login was run. Obtain the repaired
server diagnostic before deciding whether this is TLS trust, connectivity
or LDAP authentication; do not include passwords or private settings.
