services:

  PARAMETER:
    # ../update-wekan-version.sh updates this WeKan version number
    image: ghcr.io/wekan/wekan:v9.20
    container_name: PARAMETER-tenant
    restart: unless-stopped
    network_mode: "host"
    environment:
      # ========== CUSTOM START =====
