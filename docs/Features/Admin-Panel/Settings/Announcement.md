# Admin Panel / Settings / Announcement

One instance-wide banner: a checkbox to show it, and the text.

The banner can be **dismissed permanently per user** — closing it stores the current
announcement version on that user, so it does not come back on reload or when
switching boards. Editing the text changes its version, and the banner reappears for
everyone.
